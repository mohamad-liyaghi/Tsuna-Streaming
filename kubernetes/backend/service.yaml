apiVersion: v1
kind: Service
metadata:
  name: tsuna-streaming-backend-service
spec:
    selector:
        app: tsuna-streaming-backend
    ports:
        - protocol: TCP
          port: 8000
          targetPort: 8000
    type: LoadBalancer
